{
  "name": "@priority-cpa/ocr-azure",
  "version": "0.1.0",
  "private": true,
  "description": "Extracts invoice fields from a PDF using Azure Document Intelligence (with deterministic mock fallback when credentials are absent).",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@priority-cpa/invoice-schema": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
