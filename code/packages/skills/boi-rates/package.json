{
  "name": "@priority-cpa/boi-rates",
  "version": "0.1.0",
  "private": true,
  "description": "Fetches daily FX rates from Bank of Israel's public API (with deterministic mock fallback when the API is unavailable).",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
