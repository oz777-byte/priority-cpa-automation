{
  "name": "@priority-cpa/scenario-detector",
  "version": "0.1.0",
  "private": true,
  "description": "Detects which JE scenario applies to an invoice (12 scenarios from the playbook).",
  "type": "module",
  "exports": { ".": "./src/index.ts" },
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@priority-cpa/invoice-schema": "*",
    "@priority-cpa/israeli-vat-logic": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
